package app.thirtyninth.githubviewer.utils

enum class UIState {
    NORMAL,
    LOADING,
    ERROR
}

enum class TokenState {
    CORRECT,
    INVALID,
    EMPTY
}

enum class LoginState {
    CORRECT,
    INVALID,
    EMPTY
}
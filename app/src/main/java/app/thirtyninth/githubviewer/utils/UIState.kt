package app.thirtyninth.githubviewer.utils

enum class UIState {
    NORMAL,
    LOADING,
    SUCCESS,
    ERROR
}

enum class TokenState {
    CORRECT,
    INVALID,
    EMPTY
}

enum class UsernameState {
    CORRECT,
    INVALID,
    EMPTY
}